#!/bin/bash
if ! command -v wget > /dev/null 2>&1; then
    echo "wget is not installed but is needed for the program to work."
    exit 1
fi
KEEP=false
YES=false
GLOBAL=true
DIR="/usr/local/lib"
while [ $# -ne 0 ]; do
    case "$1" in
        -h | --help)
            echo -e "pwsh-update: Update/install PowerShell on Linux distros
Usage:
-h | --help
    Print a help message and exit.

-k | --keep
    Keep the downloaded archive after installation.

-y | --yes
    Answer \"y\" for everything.

-u | --user
    Only install for user.

-d | --directory [dir]
    Install in [dir]. Must be used with the \"--user\" option."
            exit
            ;;
        -k | --keep)
            KEEP=true
            ;;
        -y | --yes)
            YES=true
            ;;
        -u | --user)
            GLOBAL=false
            if [ "$DIR" = "/usr/local/lib" ]; then
                DIR="$HOME/powershell"
            fi
            ;;
        -d | --directory)
            if [ $# = 1 ]; then
                echo "Missing argument: [dir]"
                exit 1
            fi
            DIR=$(realpath -- "$2")
            shift
            ;;
        *)
            echo "\"$1\": invalid option. Run \"pwsh-update --help\" to see all options."
            exit 1
            ;;
    esac
    shift
done
if [ $EUID != 0 ] && [ $GLOBAL = "true" ]; then
    echo "Please run this script with root privileges or the \"--user\" option."
    exit 1
fi
if [ $GLOBAL = "true" ] && [ "$DIR" != "/usr/local/bin" ]; then
    echo "You need to use the \"--user\" option with the \"--directory\" option."
    exit 1
fi
if [ $GLOBAL = "false" ]; then
    case "$DIR" in
        "$HOME/"*)
            ;;
        *)
            echo "You must install PowerShell in your home directory."
            exit 1
            ;;
    esac
fi
echo "Installing PowerShell under directory \"$DIR\"."
case "$(arch)" in
    aarch64)
        ARCH="arm64"
        ;;
    arm)
        ARCH="arm32"
        ;;
    x86_64 | amd64)
        ARCH="x64"
        ;;
    *)
        echo "Architecture \"$(arch)\" not supported."
        exit 1
        ;;
esac
LATEST=$(wget -qO- https://github.com/PowerShell/PowerShell/releases/latest | grep -oh "<title>.*</title>" | tr " " "\n" | grep "^v.*$" | tr -d "v")
if [ "$LATEST" = "" ]; then
    echo "Failed to get latest PowerShell version."
    exit 1
fi
if command -v "$DIR/pwsh" > /dev/null 2>&1; then
    if [ "$LATEST" = "$("$DIR/pwsh" -v | sed -e "s/PowerShell //g")" ]; then
        echo "PowerShell is already the newest version ($LATEST)."
        exit
    else
        echo -n "PowerShell version ($LATEST) is released! Update? [y/n] "
    fi
else
    echo -n "PowerShell is not installed. Install? [y/n] "
fi
if [ $YES = "true" ]; then
    CHOICE="Y"
    echo "y"
else
    read -r CHOICE
fi
if [ "${CHOICE^^}" = "N" ]; then
    echo "Abort."
else
    TARFILE=$(mktemp /tmp/powershell-XXXXXX.tar.gz)
    echo "Downloading PowerShell ($LATEST)..."
    if wget "https://github.com/PowerShell/PowerShell/releases/download/v$LATEST/powershell-$LATEST-linux-$ARCH.tar.gz" -O "$TARFILE" > /dev/null 2>&1; then
        echo "Download done."
    else
        echo "Download failed."
        exit 1
    fi
    echo "Extracting files..."
    if rm -rf "$DIR" > /dev/null 2>&1; mkdir -p "$DIR" > /dev/null 2>&1; tar -zxf "$TARFILE" -C "$DIR" > /dev/null 2>&1; then
        if [ $GLOBAL = "true" ]; then
            echo "Extraction done."
            echo "Linking executable to \"/usr/local/bin\"..."
            if ln -sf /usr/local/lib/powershell/pwsh /usr/local/bin/pwsh > /dev/null 2>&1; then
                echo "Linking done. Make sure that \"/usr/local/bin\" is in your \$PATH. Run \"pwsh\" to start PowerShell."
            else
                echo "Linking failed."
                exit 1
            fi
        else
            echo "Extraction done. Run \"$DIR/pwsh\" to start PowerShell."
        fi
    else
        echo "Extraction failed."
        exit 1
    fi
    if [ $KEEP = "true" ]; then
        echo "The archive is stored at \"$TARFILE\". Move it to another directory for later use."
    else
        rm -f "$TARFILE"
    fi
fi
